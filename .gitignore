node_modules/
dist/
coverage/
*.log
.env
.env.local
.DS_Store

# Tally exports contain personal data — never commit
*.csv

# Saved runs and published-map backups. These carry member names and the paragraphs people
# wrote, so the contents are never committed — but the directory IS tracked, so a fresh clone
# has an obvious place to put them that isn't a folder you empty (Downloads, Desktop, /tmp).
/backups/*
!/backups/README.md

# Personal Obsidian vaults staged here temporarily — never commit
/Joris Vault/
/Obsidian vault/
